export interface Presentation {
  id: number;
  name: string;
  quantity: number;
  measure: string;
  measure_value: number;
}

export interface PostPresentation {
  name: string;
  quantity: number;
  measure: string;
  measure_value: number;
}
